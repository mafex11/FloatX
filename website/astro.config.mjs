import { defineConfig } from 'astro/config';

// Static landing page. Set `site` to the deployed URL when wiring up
// Cloudflare Pages / GitHub Pages.
export default defineConfig({
  site: 'https://floatx.pages.dev',
});
